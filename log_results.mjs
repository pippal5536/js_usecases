/**
 * Logs result to the console.
 * @param {*} result - The result to log, can be of any type.
 * @returns {void} - No return value.
 */
const log_results = (result) => console.log({ result });

export { log_results };
